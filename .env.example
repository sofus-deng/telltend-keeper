# Copy to .env only for local development if your shell tooling loads it.
# Never commit real credentials or AWS access keys.

AWS_REGION=us-west-2
KEEPER_BEDROCK_MODEL_ID=global.anthropic.claude-sonnet-4-6
KEEPER_STATE_PATH=.keeper/runtime.json
KEEPER_SOURCE_VERSION=development
PORT=8080
