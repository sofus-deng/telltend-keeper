{
  "name": "telltend-keeper",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A governed website-operations agent for the Agents for Humans Hackathon.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "agent": "tsx src/agent.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "check": "npm run typecheck && npm run build && npm run test",
    "test": "vitest run"
  },
  "dependencies": {
    "@strands-agents/sdk": "1.11.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "tsx": "4.23.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
