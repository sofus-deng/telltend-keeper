{
  "name": "telltend-keeper",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A governed website-operations agent for the Agents for Humans Hackathon.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "agent": "tsx src/agent.ts",
    "agentcore": "node dist-agentcore/app.js",
    "build": "tsc -p tsconfig.json",
    "build:agentcore": "node scripts/build-agentcore.mjs",
    "smoke:agentcore": "node scripts/smoke-agentcore.mjs",
    "typecheck": "tsc -p tsconfig.test.json",
    "check": "npm run typecheck && npm run build && npm run build:agentcore && npm run smoke:agentcore && npm run test",
    "test": "vitest run"
  },
  "dependencies": {
    "@strands-agents/sdk": "1.11.2",
    "express": "5.2.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/express": "5.0.6",
    "@types/node": "22.20.1",
    "esbuild": "0.28.1",
    "tsx": "4.23.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
